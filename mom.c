// Multi-Orbital Model

#include <stdio.h>
#include <stdlib.h>
#include <math.h>
#include <time.h>
#include <lapack.h>
#include <omp.h>

int main(int argc, char *argv[]) {
	if(argc != 1) {
		printf("Usage : %s\n", argv[0]);
		exit(1);
	}

	return 0;
}
